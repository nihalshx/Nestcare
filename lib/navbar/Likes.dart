import 'package:flutter/material.dart';

class Likes_page extends StatelessWidget {
  const Likes_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Likes')),
    );
  }
}
